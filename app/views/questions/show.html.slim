
p
  | Просмотр вопроса с ответами!

h3= "Вопрос:  "
p= @question.title
- if current_user.present?
  = link_to 'Delete question', question_path(@question), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }

h4= "Описание вопроса:  "
= @question.body
    
h4= "Все ответы на вопрос: "
= render partial: 'answers/answers', collection: @question.answers

.answer-errors
  = render 'shared/errors', resource: @answer
  
= render 'answers/form', collection: @answer

p= link_to 'Back', questions_path